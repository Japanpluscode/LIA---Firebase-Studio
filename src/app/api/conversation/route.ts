// This empty file is a placeholder because Next.js requires a `route.ts` 
// for API routes, but the WebSocket server is handled in `server.ts`.
// We do not need to add any code here.
export async function GET() {
  return new Response(null, { status: 200 });
}
